services:
  - type: web
    name: compactador
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y gcc g++ zip
      pip install -r requirements.txt
    startCommand: gunicorn app:app